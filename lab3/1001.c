#include <stdio.h>

int main() {
    // Declare variables A, B, and X
    int A, B, X;

    // Read variables A and B

    scanf("%d%d",&A, &B);

    // Calculate the sum
    X = A + B;

    // Print the result with an endline
    printf("X = %d\n", X);

    return 0;
}


